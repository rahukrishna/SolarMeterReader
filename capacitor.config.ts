import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.solarmeterreader.app',
  appName: 'Solar Meter Reader',
  webDir: 'dist'
};

export default config;
